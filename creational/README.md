Small examples of creational patterns in Java:
- Factory Method
- Prototype

To start compile from this folder with (your shell must support `**` expansion):

``` sh
javac **/*.java
```

And run with:

``` sh
java composite.CompositeTest
// or
java proxy.ProxyTest
```

